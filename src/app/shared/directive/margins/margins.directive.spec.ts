import { MarginsDirective } from './margins.directive';

describe('MarginsDirective', () => {
  it('should create an instance', () => {
    const directive = new MarginsDirective();
    expect(directive).toBeTruthy();
  });
});
