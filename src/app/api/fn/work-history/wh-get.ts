/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WorkHistoryListResponse as DtoWorkHistoryListResponse } from '../../models/dto/work-history-list-response';

export interface WhGet$Params {

/**
 * Номер страницы
 */
  page?: number;

/**
 * Размер страницы
 */
  size?: number;
}

export function whGet(http: HttpClient, rootUrl: string, params?: WhGet$Params, context?: HttpContext): Observable<StrictHttpResponse<DtoWorkHistoryListResponse>> {
  const rb = new RequestBuilder(rootUrl, whGet.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DtoWorkHistoryListResponse>;
    })
  );
}

whGet.PATH = '/wh';
