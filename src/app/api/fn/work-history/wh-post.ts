/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WorkHistoryDto as DtoWorkHistoryDto } from '../../models/dto/work-history-dto';

export interface WhPost$Params {
  
    /**
     * Данные записи
     */
    body: {
} & {
'about'?: string;
'logoUrl'?: string;
'name'?: string;
'periodEnd'?: string;
'periodStart'?: string;
'projects'?: Array<string>;
'whatIDid'?: Array<string>;
}
}

export function whPost(http: HttpClient, rootUrl: string, params: WhPost$Params, context?: HttpContext): Observable<StrictHttpResponse<DtoWorkHistoryDto>> {
  const rb = new RequestBuilder(rootUrl, whPost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DtoWorkHistoryDto>;
    })
  );
}

whPost.PATH = '/wh';
