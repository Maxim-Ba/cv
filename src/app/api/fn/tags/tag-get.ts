/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TagListResponse as DtoTagListResponse } from '../../models/dto/tag-list-response';

export interface TagGet$Params {

/**
 * Номер страницы
 */
  page?: number;

/**
 * Размер страницы
 */
  size?: number;
}

export function tagGet(http: HttpClient, rootUrl: string, params?: TagGet$Params, context?: HttpContext): Observable<StrictHttpResponse<DtoTagListResponse>> {
  const rb = new RequestBuilder(rootUrl, tagGet.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DtoTagListResponse>;
    })
  );
}

tagGet.PATH = '/tag';
